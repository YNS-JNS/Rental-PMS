---
description: Closes the session, executes the verify protocol, and saves the context to memory.md.
---

The session is over. Execute the <PROTOCOL:VERIFY> phase and save the current state of the architecture, the decisions we made today, and any modified dependencies into the docs/memory.md file to prevent amnesia for our next session.